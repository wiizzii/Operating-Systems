The linked list is implemented as intended as a simple pointer list.

Everything works as it should.
design choises:
The number of vertecies must not be larger than 255 digits.
checkProgram is giving error because a , at the end of the output that is not in the exspected output where there are no cycles.
